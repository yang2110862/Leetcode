#include "header.h"

class Solution {
public:
    int numberOfArithmeticSlices(vector<int>& nums) {
        int n = nums.size();
        vector<int> diff(n);
        for (int i = 1; i < n; ++i) {
            
        }
    }
};